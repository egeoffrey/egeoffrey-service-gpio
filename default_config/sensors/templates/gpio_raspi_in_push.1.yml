disabled: true
description: gpio_raspi_in_passive
format: int
retain: history
calculate: avg_min_max
service:
  configuration:
    pin: 3
    edge_detect: both
    pull_up_down: up
  mode: push
  name: gpio_raspi