disabled: true
description: gpio_orangepi_in_passive
format: int
retain: history
calculate: avg_min_max
service:
  configuration:
    pin: 3
    edge_detect: both
  mode: passive
  name: gpio_orangepi